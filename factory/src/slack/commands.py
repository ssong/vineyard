"""Slack slash command handlers for factory.

Note: Command handling is done in the top-level main.py which registers
the unified /vineyard command handler. This module is kept for backward
compatibility with the factory's own main.py entry point but contains
no active handlers.
"""
